public class LessenSixteen {
/*

Задача 3: Безопасный ввод числа
Создайте метод inputNumber( ),
который просит пользователя ввести целое число.
Если пользователь вводит не число (буквы, символы),
программа должна перехватить исключение,
вывести сообщение об ошибке и повторить запрос. Метод возвращает корректно введённое число.

Задача 4: Конвертер строки в число
Создайте метод parseNumber(String str), который преобразует строку в целое число. Обработайте NumberFormatException - если строка не является числом, выведите сообщение об ошибке и верните 0. Протестируйте метод со строками: "123", "abc", "12.5".

Задача 5: Калькулятор с исключениями
Создайте метод calculate(double a, double b, char operation), который выполняет арифметические операции (+, -, *, /). Обработайте деление на ноль. Если передана неизвестная операция (например, '%'), бросьте исключение IllegalArgumentException с сообщением "Neizvestnaya operaciya".

Задача 6: Валидация возраста
Создайте метод validateAge(int age), который проверяет корректность возраста (должен быть от 0 до 150). Если возраст отрицательный или больше 150, бросьте IllegalArgumentException с соответствующим сообщением. В методе main обработайте это исключение и выведите сообщение об ошибке.

Задача 7: Множественные исключения
Создайте метод processArray(int[] arr, int index, int divisor), который берёт элемент массива по индексу и делит его на divisor. Метод должен обрабатывать три типа исключений отдельными catch блоками:

NullPointerException - если массив null
ArrayIndexOutOfBoundsException - если индекс неверный
ArithmeticException - если делитель равен 0

При каждой ошибке выводите своё сообщение. Возвращайте результат деления или -1 при ошибке.
     */
//Задача 1: Деление на ноль
//Создайте метод safeDivision(int a, int b), который делит два числа и обрабатывает деление на ноль через try-catch.
// Метод должен возвращать результат деления или выводить сообщение об ошибке и возвращать 0.
    static int safeDivision(int a, int b){
        try {
            return a/b;
        } catch (ArithmeticException e){
            //нам нужно самим написать? Не совсем понятно по заданию. Тогда напишем так:
            //System.out.println("Деление на ноль запрещено!");
            //или отправим в поток ERROR
            //System.err.println("Деление на ноль запрещено!");
            e.printStackTrace();
            return 0;
        }
    }
    //Задача 2: Проверка массива
    //Создайте метод getElement(int[] arr, int index), который возвращает элемент массива по индексу.
    // Обработайте ситуацию, когда индекс выходит за границы массива (ArrayIndexOutOfBoundsException). При ошибке возвращайте -1.
static int getElement(int[] arr, int index){

}

    public static void main(String[] args) {
        System.out.println(safeDivision(2,0));
    }
}
